//Declare variables for a Flipkart product: productName (as a string), price (float), and rating (double). Assign sample values and print each variable with its data type.
#include<stdio.h>
 main()
 
 {
 	char ProductName[]="laptop";
 	float price=120000;
 	double rating=4.5;
 	
 	printf("\nWelcome to Flipkart");
 	
 	printf("\nproduct name is(STRING):%s",ProductName);
 	printf("\nproduct price is(FLOAT):%.0f",price);
 	printf("\nrating of product is(DOUBLE):%.1lf",rating);

 	
 	
 	
 }
